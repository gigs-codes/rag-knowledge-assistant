fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

pypdf==5.1.0
python-docx==1.1.2
langchain-text-splitters==0.3.4

sentence-transformers==3.3.1
chromadb==0.5.23

requests==2.32.3

pytest==8.3.4

sqlalchemy==2.0.36
psycopg2-binary==2.9.10

langgraph==0.2.62
# Pinned explicitly, not left as a transitive resolve: installing the
# official `mcp` SDK during development pulled in a Starlette version
# incompatible with fastapi==0.115.6 and broke the app (that's why the
# MCP server in mcp_server/ is hand-built over stdio instead of using
# that SDK). Pinning here keeps `pip install` deterministic regardless of
# what else gets added later, rather than depending on resolver ordering.
starlette==0.41.3

# Auth (JWT + password hashing)
pyjwt==2.10.1
bcrypt==4.2.1

# Hybrid retrieval (BM25 keyword search + cross-encoder reranking)
rank-bm25==0.2.2

# Multimodal ingestion (CSV/XLSX + PDF table extraction) — pure Python,
# no OCR/system binaries required
openpyxl==3.1.5
pdfplumber==0.11.4

# Agent conversation persistence (survives backend restarts). Pinned to
# 2.0.11, not latest — the newest langgraph-checkpoint-sqlite pulls in
# langgraph-checkpoint 4.x, which conflicts with langgraph==0.2.62's own
# pin (langgraph-checkpoint<3.0.0). 2.0.11 resolves to langgraph-checkpoint
# 2.1.2, which satisfies both. Same "conflicting transitive pin" situation
# already documented for starlette above.
langgraph-checkpoint-sqlite==2.0.11
